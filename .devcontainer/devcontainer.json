{
  "name": "GemRB Dev",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "customizations": {
    "vscode": {
        "extensions": [
            "ms-vscode.cpptools",
            "ms-python.python",
            "ms-vscode.cmake-tools"
        ]
    }
  },
  "postCreateCommand": "rm -rf build && cmake -S . -B build -DCMAKE_INSTALL_PREFIX=/workspaces/gemrb/install && cmake --build build -- -j$(nproc) && rm -rf install && cmake --install build",
  "remoteUser": "vscode"
}